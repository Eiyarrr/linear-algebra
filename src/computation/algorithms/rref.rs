/*
 * Take in matrix
 * Set topleft to 1
 *  if exists, continue
 *  else keep sending to back row
 *      if none of the rows have one, move one right
 * Make column of 0s
 * Move down and right one
 * Repeat
 */
